# Phase 1 Demo 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 构建 Agent Village Phase 1 Demo，包含农夫+酒保两个NPC Agent、手动时间推进、玩家对话交互、状态系统、耕作工具、农夫乐事结局、Token硬隔离。

**架构：** 混合模式后端（单体进程+内部消息总线抽象），Vue 3 SPA前端，通过REST API+WebSocket通信。数据存储使用本地JSON文件，LLM调用使用DeepSeek API。

**技术栈：** Python 3.10+ / FastAPI / Uvicorn / Vue 3 / Vite / Pinia / DeepSeek API

---

## 文件结构

```
agentVillage/
├── server/                          # Python 后端
│   ├── main.py                      # FastAPI 入口
│   ├── config.py                    # 配置常量（token预算、状态值等）
│   ├── models/
│   │   ├── __init__.py
│   │   ├── game_time.py             # GameTime 类
│   │   ├── npc_state.py             # NPCState + 状态描述映射
│   │   ├── player_state.py          # PlayerState + 权限过滤
│   │   └── messages.py              # Message / DialogueTurn 类型
│   ├── core/
│   │   ├── __init__.py
│   │   ├── orchestrator.py          # 主控Agent
│   │   ├── time_system.py           # 时间推进 + tick触发
│   │   ├── message_bus.py           # 内存消息总线
│   │   ├── state_store.py           # StateStore接口 + JsonStore
│   │   └── ending_checker.py        # 结局条件判定
│   ├── agents/
│   │   ├── __init__.py
│   │   ├── base_agent.py            # NPCAgent 基类
│   │   ├── farmer.py                # 农夫Agent
│   │   └── bartender.py             # 酒保Agent
│   ├── llm/
│   │   ├── __init__.py
│   │   ├── client.py                # DeepSeek LLM客户端 + 异步队列
│   │   ├── token_budget.py          # Token预算追踪
│   │   └── context_builder.py       # NPC上下文组装
│   ├── tools/
│   │   ├── __init__.py
│   │   ├── base_tool.py             # Tool 基类
│   │   └── farming.py               # 耕作工具
│   ├── memory/
│   │   ├── __init__.py
│   │   └── memory_manager.py        # 基础文件记忆管理
│   ├── api/
│   │   ├── __init__.py
│   │   ├── routes.py                # REST 路由
│   │   └── websocket.py             # WebSocket 处理
│   └── data/
│       └── npc_backgrounds/
│           ├── farmer.yaml            # 农夫背景设定
│           └── bartender.yaml         # 酒保背景设定
├── client/                          # Vue 3 前端
│   ├── package.json
│   ├── vite.config.ts
│   ├── tsconfig.json
│   └── src/
│       ├── main.ts
│       ├── App.vue
│       ├── components/
│       │   ├── ChatPanel.vue        # 对话面板
│       │   ├── StatusPanel.vue      # 玩家状态面板
│       │   ├── TimeControl.vue      # 时间控制面板
│       │   ├── ToolPanel.vue        # 工具面板
│       │   └── NPCPanel.vue         # NPC状态面板
│       ├── stores/
│       │   └── gameStore.ts         # Pinia store
│       ├── services/
│       │   ├── api.ts               # REST API 封装
│       │   └── websocket.ts         # WebSocket 封装
│       └── types/
│           └── index.ts             # TypeScript 类型
├── tests/                           # 测试目录
│   └── server/
│       ├── test_models.py
│       ├── test_orchestrator.py
│       ├── test_context_builder.py
│       └── test_farming_tool.py
└── docs/
    └── ... (设计文档)
```

---

## 任务清单

### 任务 1：后端项目脚手架

**文件：**
- 创建：`server/main.py`
- 创建：`server/config.py`
- 创建：`server/requirements.txt`
- 创建：`.gitignore`（补充Python相关）

- [ ] **步骤 1：创建 requirements.txt**

```txt
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.0
pyyaml==6.0.1
httpx==0.26.0
pytest==8.0.0
pytest-asyncio==0.23.0
websockets==12.0
python-dotenv==1.0.0
```

- [ ] **步骤 2：创建 config.py**

```python
from dataclasses import dataclass

@dataclass
class GameConfig:
    INTERACTIONS_PER_HOUR: int = 3
    INTERACTION_COOLDOWN_MINUTES: int = 10
    NPC_STATE_DECAY_HUNGER: int = 5
    NPC_STATE_DECAY_FATIGUE: int = 5
    TOKEN_WARNING_THRESHOLD: float = 0.8
    TOKEN_HARD_LIMIT: float = 1.0
    LLM_MAX_CONCURRENT: int = 3
    INPUT_MAX_LENGTH: int = 500
    FARMING_FATIGUE_COST: int = 20
    FARMING_HUNGER_MIN: int = 20
    FARMING_FATIGUE_MAX: int = 80
    FARMER_JOY_AFFINITY: int = 80
    FARMER_JOY_CROP_TYPES: int = 2
    FARMER_JOY_FARM_COUNT: int = 5

config = GameConfig()
```

- [ ] **步骤 3：创建 main.py（最小FastAPI应用）**

```python
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware

app = FastAPI(title="Agent Village")

app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:5173"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

@app.get("/health")
async def health():
    return {"status": "ok"}
```

- [ ] **步骤 4：安装依赖并验证**

```bash
cd server
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
uvicorn main:app --reload
```

浏览器访问 `http://localhost:8000/health` 应返回 `{"status":"ok"}`

- [ ] **步骤 5：Commit**

```bash
git add server/requirements.txt server/config.py server/main.py .gitignore
git commit -m "chore: 后端脚手架 - FastAPI + 配置 + 依赖"
```

---

### 任务 2：核心数据模型

**文件：**
- 创建：`server/models/game_time.py`
- 创建：`server/models/npc_state.py`
- 创建：`server/models/player_state.py`
- 创建：`server/models/messages.py`
- 测试：`tests/server/test_models.py`

- [ ] **步骤 1：编写 GameTime 模型**

```python
# server/models/game_time.py
from dataclasses import dataclass

@dataclass
class GameTime:
    day: int = 1
    hour: int = 6
    minute: int = 0

    def tick(self, minutes: int = 60) -> None:
        self.minute += minutes
        while self.minute >= 60:
            self.minute -= 60
            self.hour += 1
        while self.hour >= 24:
            self.hour -= 24
            self.day += 1

    def to_dict(self) -> dict:
        return {"day": self.day, "hour": self.hour, "minute": self.minute}

    @classmethod
    def from_dict(cls, data: dict) -> "GameTime":
        return cls(day=data["day"], hour=data["hour"], minute=data["minute"])

    def __str__(self) -> str:
        return f"Day {self.day}, {self.hour:02d}:{self.minute:02d}"
```

- [ ] **步骤 2：编写测试**

```python
# tests/server/test_models.py
import pytest
from server.models.game_time import GameTime

def test_game_time_tick():
    t = GameTime(day=1, hour=23, minute=0)
    t.tick(120)
    assert t.day == 2
    assert t.hour == 1
    assert t.minute == 0
```

- [ ] **步骤 3：运行测试**

```bash
cd d:/work_ai/agentVillage
pytest tests/server/test_models.py -v
```

预期：PASS

- [ ] **步骤 4：编写 NPCState 模型**

```python
# server/models/npc_state.py
from dataclasses import dataclass
from typing import Dict

@dataclass
class NPCState:
    health: int = 100
    hunger: int = 100
    fatigue: int = 0
    mood: int = 50

    def describe(self) -> Dict[str, str]:
        return {
            "health": self._describe_value(self.health, [
                (0, 20, "你感觉身体极度虚弱"),
                (21, 40, "你身体有些不适"),
                (41, 60, "你身体状况一般"),
                (61, 80, "你感觉身体不错"),
                (81, 100, "你感觉精力充沛，非常健康"),
            ]),
            "hunger": self._describe_value(self.hunger, [
                (0, 20, "你饿得头晕眼花"),
                (21, 40, "你的肚子在咕咕叫"),
                (41, 60, "你不太饿也不太饱"),
                (61, 80, "你吃得还算饱"),
                (81, 100, "你吃得很饱很满足"),
            ]),
            "fatigue": self._describe_value(self.fatigue, [
                (0, 20, "你精神抖擞"),
                (21, 40, "你精神还不错"),
                (41, 60, "你有些累了"),
                (61, 80, "你感到很疲倦"),
                (81, 100, "你疲惫到几乎无法站立"),
            ]),
        }

    def _describe_value(self, value: int, ranges) -> str:
        for lo, hi, desc in ranges:
            if lo <= value <= hi:
                return desc
        return ranges[0][2] if value < ranges[0][0] else ranges[-1][2]
```

- [ ] **步骤 5：编写 PlayerState 模型**

```python
# server/models/player_state.py
from dataclasses import dataclass, field
from typing import Dict, List

@dataclass
class PlayerState:
    name: str = ""
    health: int = 100
    hunger: int = 100
    fatigue: int = 0
    location: str = "village_entrance"
    gold: int = 10
    items: List[str] = field(default_factory=list)
    relationships: Dict[str, int] = field(default_factory=dict)
    reputation: int = 50
    lawful_score: int = 50
    farm_count: int = 0
    crops_harvested: List[str] = field(default_factory=list)

    def get_visible_state(self, npc_visibility: List[str]) -> dict:
        result = {
            "basic": {
                "name": self.name,
                "health": self.health,
                "hunger": self.hunger,
                "fatigue": self.fatigue,
                "location": self.location,
            }
        }
        if "social" in npc_visibility:
            result["social"] = {
                "relationships": self.relationships,
                "reputation": self.reputation,
            }
        if "wealth" in npc_visibility:
            result["wealth"] = {
                "gold": self.gold,
                "items": self.items,
            }
        return result
```

- [ ] **步骤 6：编写 DialogueTurn 模型**

```python
# server/models/messages.py
from dataclasses import dataclass
from typing import Optional, List

@dataclass
class DialogueTurn:
    speaker: str
    content: str
    timestamp: Optional[str] = None
    options: Optional[List[str]] = None
```
```

- [ ] **步骤 7：运行所有模型测试**

```bash
pytest tests/server/test_models.py -v
```

预期：PASS

- [ ] **步骤 8：Commit**

```bash
git add server/models/ tests/server/test_models.py
git commit -m "feat: 核心数据模型 - GameTime, NPCState, PlayerState, DialogueTurn"
```

---

### 任务 3：状态存储（JsonStore）

**文件：**
- 创建：`server/core/state_store.py`
- 测试：`tests/server/test_state_store.py`

- [ ] **步骤 1：编写 StateStore 接口 + JsonStore 实现**

```python
# server/core/state_store.py
from abc import ABC, abstractmethod
import json
import os
from typing import Any, Dict

class StateStore(ABC):
    @abstractmethod
    def save(self, key: str, data: Dict[str, Any]) -> None: ...

    @abstractmethod
    def load(self, key: str) -> Dict[str, Any] | None: ...

    @abstractmethod
    def delete(self, key: str) -> None: ...

class JsonStore(StateStore):
    def __init__(self, base_path: str = "data/users/default"):
        self.base_path = base_path
        os.makedirs(self.base_path, exist_ok=True)

    def _path(self, key: str) -> str:
        return os.path.join(self.base_path, f"{key}.json")

    def save(self, key: str, data: Dict[str, Any]) -> None:
        with open(self._path(key), "w", encoding="utf-8") as f:
            json.dump(data, f, ensure_ascii=False, indent=2)

    def load(self, key: str) -> Dict[str, Any] | None:
        p = self._path(key)
        if not os.path.exists(p):
            return None
        with open(p, "r", encoding="utf-8") as f:
            return json.load(f)

    def delete(self, key: str) -> None:
        p = self._path(key)
        if os.path.exists(p):
            os.remove(p)
```

- [ ] **步骤 2：编写测试**

```python
# tests/server/test_state_store.py
import os
import pytest
from server.core.state_store import JsonStore

def test_json_store_save_load():
    store = JsonStore(base_path="data/test")
    store.save("test_key", {"a": 1, "b": "hello"})
    loaded = store.load("test_key")
    assert loaded == {"a": 1, "b": "hello"}
    store.delete("test_key")
    assert store.load("test_key") is None
```

- [ ] **步骤 3：运行测试**

```bash
pytest tests/server/test_state_store.py -v
```

预期：PASS

- [ ] **步骤 4：Commit**

```bash
git add server/core/state_store.py tests/server/test_state_store.py
git commit -m "feat: JsonStore 状态存储"
```

---

### 任务 4：Token 预算追踪

**文件：**
- 创建：`server/llm/token_budget.py`
- 测试：`tests/server/test_token_budget.py`

- [ ] **步骤 1：编写 TokenBudget**

```python
# server/llm/token_budget.py
from dataclasses import dataclass
from enum import Enum

class BudgetStatus(Enum):
    NORMAL = "normal"
    WARNING = "warning"
    EXHAUSTED = "exhausted"

@dataclass
class TokenBudget:
    daily_limit: int = 5000
    used: int = 0
    warning_threshold: float = 0.8

    def consume(self, tokens: int) -> bool:
        if self.status == BudgetStatus.EXHAUSTED:
            return False
        self.used += tokens
        return True

    @property
    def status(self) -> BudgetStatus:
        ratio = self.used / self.daily_limit
        if ratio >= 1.0:
            return BudgetStatus.EXHAUSTED
        if ratio >= self.warning_threshold:
            return BudgetStatus.WARNING
        return BudgetStatus.NORMAL

    def reset(self) -> None:
        self.used = 0
```

- [ ] **步骤 2：编写测试**

```python
# tests/server/test_token_budget.py
from server.llm.token_budget import TokenBudget, BudgetStatus

def test_token_budget_exhaustion():
    budget = TokenBudget(daily_limit=100)
    assert budget.status == BudgetStatus.NORMAL
    budget.consume(80)
    assert budget.status == BudgetStatus.WARNING
    budget.consume(20)
    assert budget.status == BudgetStatus.EXHAUSTED
    assert budget.consume(1) == False
```

- [ ] **步骤 3：运行测试**

```bash
pytest tests/server/test_token_budget.py -v
```

预期：PASS

- [ ] **步骤 4：Commit**

```bash
git add server/llm/token_budget.py tests/server/test_token_budget.py
git commit -m "feat: Token预算硬隔离"
```

---

### 任务 5：LLM 客户端

**文件：**
- 创建：`server/llm/client.py`

- [ ] **步骤 1：编写 LLMClient**

```python
# server/llm/client.py
import os
import asyncio
from typing import List, Dict, Any
import httpx

class LLMClient:
    def __init__(self, api_key: str | None = None, base_url: str = "https://api.deepseek.com"):
        self.api_key = api_key or os.getenv("DEEPSEEK_API_KEY", "")
        self.base_url = base_url
        self.client = httpx.AsyncClient(timeout=60.0)
        self.semaphore = asyncio.Semaphore(3)

    async def chat(self, messages: List[Dict[str, str]], model: str = "deepseek-chat") -> Dict[str, Any]:
        async with self.semaphore:
            resp = await self.client.post(
                f"{self.base_url}/v1/chat/completions",
                headers={"Authorization": f"Bearer {self.api_key}", "Content-Type": "application/json"},
                json={"model": model, "messages": messages, "temperature": 0.7},
            )
            resp.raise_for_status()
            return resp.json()

    async def chat_with_retry(self, messages: List[Dict[str, str]], model: str = "deepseek-chat", retries: int = 1) -> Dict[str, Any]:
        for attempt in range(retries + 1):
            try:
                return await self.chat(messages, model)
            except Exception:
                if attempt == retries:
                    raise
                await asyncio.sleep(1)
```

- [ ] **步骤 2：验证（需要DEEPSEEK_API_KEY环境变量）**

```bash
cd server
python -c "from llm.client import LLMClient; import asyncio; c=LLMClient(); print('client init ok')"
```

- [ ] **步骤 3：Commit**

```bash
git add server/llm/client.py
git commit -m "feat: DeepSeek LLM客户端 + 异步请求队列 + 并发控制"
```

---

### 任务 6：NPC 上下文构建器

**文件：**
- 创建：`server/llm/context_builder.py`
- 测试：`tests/server/test_context_builder.py`

- [ ] **步骤 1：编写 ContextBuilder**

```python
# server/llm/context_builder.py
from typing import List, Dict
from server.models.npc_state import NPCState

class ContextBuilder:
    @staticmethod
    def build_npc_context(
        identity: dict,
        npc_state: NPCState,
        world_events: str,
        user_summary: str,
        visible_player_state: dict,
        dialogue_history: List[dict],
        budget_status: str = "normal",
    ) -> List[Dict[str, str]]:
        descriptions = npc_state.describe()
        state_text = (
            f"你现在很{descriptions['health']}。"
            f"{descriptions['hunger']}。"
            f"{descriptions['fatigue']}。"
        )

        player_text = f"玩家当前状态：{visible_player_state}"

        context = (
            f"【世界事件】{world_events}\n"
            f"【玩家印象】{user_summary}\n"
            f"【玩家状态】{player_text}\n"
        )

        system_prompt = f"""你是{identity['name']}。{identity['daily_habits']}
{identity['core_motivation']}
{identity['speaking_style']}
注意：{identity['secret']}（这是你内心的秘密，不要直接告诉别人，除非极度信任）

你的当前状态：{state_text}

世界背景：{context}

你正在与玩家对话。请用第一人称回复，每次回复同时生成2-3个后续选项（放在[OPTIONS]标签后，每行一个选项）。"""

        if budget_status == "warning":
            system_prompt += "\n你现在有些疲惫，请简短回复。"

        messages = [{"role": "system", "content": system_prompt}]
        for turn in dialogue_history[-5:]:
            messages.append({"role": turn.get("role", "user"), "content": turn["content"]})
        return messages
```

- [ ] **步骤 2：编写测试**

```python
# tests/server/test_context_builder.py
from server.llm.context_builder import ContextBuilder
from server.models.npc_state import NPCState

def test_build_npc_context():
    identity = {
        "name": "农夫王大爷",
        "daily_habits": "日出而作",
        "core_motivation": "守住三亩地",
        "speaking_style": "说话慢条斯理",
        "secret": "年轻时是拳师",
    }
    state = NPCState(health=80, hunger=30, fatigue=60)
    ctx = ContextBuilder.build_npc_context(
        identity=identity,
        npc_state=state,
        world_events="今日无事",
        user_summary="一个陌生人",
        visible_player_state={"basic": {"health": 100}},
        dialogue_history=[],
    )
    assert ctx[0]["role"] == "system"
    assert "农夫王大爷" in ctx[0]["content"]
    assert "饿得头晕眼花" in ctx[0]["content"]
```

- [ ] **步骤 3：运行测试**

```bash
pytest tests/server/test_context_builder.py -v
```

预期：PASS

- [ ] **步骤 4：Commit**

```bash
git add server/llm/context_builder.py tests/server/test_context_builder.py
git commit -m "feat: NPC上下文构建器 + 状态描述映射"
```

---

### 任务 7：基础记忆管理

**文件：**
- 创建：`server/memory/memory_manager.py`

- [ ] **步骤 1：编写 MemoryManager**

```python
# server/memory/memory_manager.py
import os
from typing import List
from server.models.messages import DialogueTurn

class MemoryManager:
    def __init__(self, base_path: str, npc_id: str):
        self.dir = os.path.join(base_path, npc_id)
        os.makedirs(self.dir, exist_ok=True)
        os.makedirs(os.path.join(self.dir, "daily_summary"), exist_ok=True)
        for f in ["user.md", "agent_mem.md", "self.md"]:
            p = os.path.join(self.dir, f)
            if not os.path.exists(p):
                open(p, "w", encoding="utf-8").close()

    def _read(self, filename: str) -> str:
        p = os.path.join(self.dir, filename)
        if not os.path.exists(p):
            return ""
        with open(p, "r", encoding="utf-8") as f:
            return f.read()

    def _write(self, filename: str, content: str) -> None:
        p = os.path.join(self.dir, filename)
        with open(p, "w", encoding="utf-8") as f:
            f.write(content)

    def append_user_memory(self, text: str) -> None:
        current = self._read("user.md")
        self._write("user.md", current + "\n" + text)

    def get_user_summary(self) -> str:
        return self._read("user.md")[:500]  # 取前500字符作为摘要

    def add_dialogue(self, turn: DialogueTurn) -> None:
        current = self._read("user.md")
        self._write("user.md", current + f"\n[{turn.speaker}]: {turn.content}")
```

- [ ] **步骤 2：Commit**

```bash
git add server/memory/memory_manager.py
git commit -m "feat: 基础记忆管理 - 文件读写 + user.md摘要"
```

---

### 任务 8：NPC Agent 基类 + 农夫 + 酒保

**文件：**
- 创建：`server/agents/base_agent.py`
- 创建：`server/agents/farmer.py`
- 创建：`server/agents/bartender.py`
- 创建：`server/data/npc_backgrounds/farmer.yaml`
- 创建：`server/data/npc_backgrounds/bartender.yaml`
- 测试：`tests/server/test_agents.py`

- [ ] **步骤 1：编写 base_agent.py**

```python
# server/agents/base_agent.py
from typing import List, Dict, Optional
from server.models.npc_state import NPCState
from server.models.messages import DialogueTurn
from server.memory.memory_manager import MemoryManager
from server.llm.context_builder import ContextBuilder
from server.llm.token_budget import TokenBudget, BudgetStatus

class NPCAgent:
    def __init__(self, agent_id: str, identity: dict, memory_base: str, budget: TokenBudget):
        self.agent_id = agent_id
        self.identity = identity
        self.state = NPCState()
        self.memory = MemoryManager(memory_base, agent_id)
        self.budget = budget
        self.visibility = identity.get("visibility", ["basic"])
        self.dialogue_history: List[DialogueTurn] = []

    def get_visible_state(self, player_state: dict) -> dict:
        return player_state.get_visible_state(self.visibility)

    def can_interact(self, current_time) -> bool:
        # 夜间不可交互（除特殊情况）
        hour = current_time.hour
        if hour >= 22 or hour < 6:
            return False
        return True

    def on_hour_tick(self, game_time) -> None:
        self.state.hunger = max(0, self.state.hunger - 5)
        self.state.fatigue = min(100, self.state.fatigue + 5)
        # 重置每日token预算（新的一天）
        if game_time.hour == 0:
            self.budget.reset()
```

- [ ] **步骤 2：编写 farmer.yaml**

```yaml
id: farmer
name: 农夫王大爷
daily_habits: "日出而作，日落而息。中午在田边大树下吃干粮。"
core_motivation: "守住祖传的三亩地，供儿子读书。"
secret: "年轻时曾是镇上有名的拳师，因伤人入狱三年。"
speaking_style: "说话慢条斯理，爱用农谚。称呼年轻人'小伙子'。"
visibility:
  - basic
tools:
  - farming
```

- [ ] **步骤 3：编写 bartender.yaml**

```yaml
id: bartender
name: 酒馆李老板
daily_habits: "每天擦三遍柜台，晚上打烊后清点库存。"
core_motivation: "把父亲的酒馆经营下去，成为全村消息最灵通的人。"
secret: "其实是邻村派来的眼线，定期向村长汇报村里的动静。"
speaking_style: "热情圆滑，见人说人话见鬼说鬼话。"
visibility:
  - basic
  - social
tools:
  - trading
```

- [ ] **步骤 4：编写 farmer.py + bartender.py**

```python
# server/agents/farmer.py
from server.agents.base_agent import NPCAgent

class FarmerAgent(NPCAgent):
    def __init__(self, memory_base: str, budget):
        identity = {
            "id": "farmer",
            "name": "农夫王大爷",
            "daily_habits": "日出而作，日落而息。中午在田边大树下吃干粮。",
            "core_motivation": "守住祖传的三亩地，供儿子读书。",
            "secret": "年轻时曾是镇上有名的拳师，因伤人入狱三年。",
            "speaking_style": "说话慢条斯理，爱用农谚。称呼年轻人'小伙子'。",
            "visibility": ["basic"],
        }
        super().__init__("farmer", identity, memory_base, budget)
```

```python
# server/agents/bartender.py
from server.agents.base_agent import NPCAgent

class BartenderAgent(NPCAgent):
    def __init__(self, memory_base: str, budget):
        identity = {
            "id": "bartender",
            "name": "酒馆李老板",
            "daily_habits": "每天擦三遍柜台，晚上打烊后清点库存。",
            "core_motivation": "把父亲的酒馆经营下去，成为全村消息最灵通的人。",
            "secret": "其实是邻村派来的眼线，定期向村长汇报村里的动静。",
            "speaking_style": "热情圆滑，见人说人话见鬼说鬼话。",
            "visibility": ["basic", "social"],
        }
        super().__init__("bartender", identity, memory_base, budget)
```

- [ ] **步骤 5：编写测试**

```python
# tests/server/test_agents.py
from server.agents.farmer import FarmerAgent
from server.agents.bartender import BartenderAgent
from server.llm.token_budget import TokenBudget

def test_farmer_init():
    agent = FarmerAgent("data/test_memory", TokenBudget(daily_limit=1000))
    assert agent.agent_id == "farmer"
    assert "basic" in agent.visibility

def test_bartender_visibility():
    agent = BartenderAgent("data/test_memory", TokenBudget(daily_limit=1000))
    assert "basic" in agent.visibility
    assert "social" in agent.visibility
```

- [ ] **步骤 6：运行测试**

```bash
pytest tests/server/test_agents.py -v
```

预期：PASS

- [ ] **步骤 7：Commit**

```bash
git add server/agents/ server/data/ tests/server/test_agents.py
git commit -m "feat: NPC Agent基类 + 农夫 + 酒保 + 背景设定"
```

---

### 任务 9：消息总线

**文件：**
- 创建：`server/core/message_bus.py`

- [ ] **步骤 1：编写 MessageBus**

```python
# server/core/message_bus.py
from typing import Dict, Callable, Any
from collections import defaultdict

class MessageBus:
    def __init__(self):
        self.handlers: Dict[str, list] = defaultdict(list)

    def subscribe(self, agent_id: str, handler: Callable[[dict], Any]) -> None:
        self.handlers[agent_id].append(handler)

    def send(self, from_id: str, to_id: str, message: dict) -> None:
        msg = {"from": from_id, "to": to_id, **message}
        for handler in self.handlers.get(to_id, []):
            handler(msg)

    def broadcast(self, message: dict) -> None:
        for handlers in self.handlers.values():
            for handler in handlers:
                handler(message)
```

- [ ] **步骤 2：Commit**

```bash
git add server/core/message_bus.py
git commit -m "feat: 内存消息总线"
```

---

### 任务 10：时间系统 + Orchestrator

**文件：**
- 创建：`server/core/time_system.py`
- 创建：`server/core/orchestrator.py`
- 修改：`server/main.py`（挂载orchestrator）
- 测试：`tests/server/test_orchestrator.py`

- [ ] **步骤 1：编写 TimeSystem**

```python
# server/core/time_system.py
from server.models.game_time import GameTime

class TimeSystem:
    def __init__(self):
        self.game_time = GameTime(day=1, hour=6, minute=0)
        self.is_paused = True  # Demo阶段默认暂停，手动推进

    def tick(self, minutes: int = 60) -> None:
        if self.is_paused:
            return
        old_hour = self.game_time.hour
        self.game_time.tick(minutes)
        # 触发整点事件
        if self.game_time.hour != old_hour:
            return True  # 表示整点
        return False

    def toggle_pause(self) -> None:
        self.is_paused = not self.is_paused
```

- [ ] **步骤 2：编写 Orchestrator**

```python
# server/core/orchestrator.py
from typing import Dict, Optional
from server.core.time_system import TimeSystem
from server.core.message_bus import MessageBus
from server.core.state_store import JsonStore
from server.models.game_time import GameTime
from server.models.player_state import PlayerState
from server.agents.farmer import FarmerAgent
from server.agents.bartender import BartenderAgent
from server.llm.token_budget import TokenBudget

class Orchestrator:
    def __init__(self, user_id: str = "default"):
        self.user_id = user_id
        self.time_system = TimeSystem()
        self.message_bus = MessageBus()
        self.store = JsonStore(base_path=f"data/users/{user_id}")
        self.npcs: Dict[str, any] = {}
        self.player_state = PlayerState(name="玩家")
        self._init_npcs()

    def _init_npcs(self) -> None:
        self.npcs["farmer"] = FarmerAgent(
            memory_base=f"data/users/{self.user_id}/memory",
            budget=TokenBudget(daily_limit=5000),
        )
        self.npcs["bartender"] = BartenderAgent(
            memory_base=f"data/users/{self.user_id}/memory",
            budget=TokenBudget(daily_limit=5000),
        )

    def advance_time(self, minutes: int = 60) -> None:
        is_hour = self.time_system.tick(minutes)
        if is_hour:
            for npc in self.npcs.values():
                npc.on_hour_tick(self.time_system.game_time)
        self._auto_save()

    def _auto_save(self) -> None:
        self.store.save("world_state", {
            "game_time": self.time_system.game_time.to_dict(),
            "is_paused": self.time_system.is_paused,
        })
        self.store.save("player_state", self.player_state.__dict__)

    def get_world_state(self) -> dict:
        return {
            "game_time": self.time_system.game_time.to_dict(),
            "is_paused": self.time_system.is_paused,
            "npcs": {
                nid: {
                    "state": {
                        "health": n.state.health,
                        "hunger": n.state.hunger,
                        "fatigue": n.state.fatigue,
                        "mood": n.state.mood,
                    }
                }
                for nid, n in self.npcs.items()
            },
        }
```

- [ ] **步骤 3：修改 main.py 挂载 orchestrator**

```python
# server/main.py（追加）
from server.core.orchestrator import Orchestrator

orch = Orchestrator()

@app.get("/world")
async def get_world():
    return orch.get_world_state()

@app.post("/time/advance")
async def advance_time(minutes: int = 60):
    orch.advance_time(minutes)
    return orch.get_world_state()

@app.post("/time/toggle")
async def toggle_pause():
    orch.time_system.toggle_pause()
    return {"is_paused": orch.time_system.is_paused}
```

- [ ] **步骤 4：编写测试**

```python
# tests/server/test_orchestrator.py
from server.core.orchestrator import Orchestrator

def test_orchestrator_init():
    o = Orchestrator("test_user")
    assert "farmer" in o.npcs
    assert "bartender" in o.npcs
    assert o.time_system.game_time.hour == 6
```

- [ ] **步骤 5：运行测试**

```bash
pytest tests/server/test_orchestrator.py -v
```

预期：PASS

- [ ] **步骤 6：验证 API**

```bash
# 启动服务
cd server && uvicorn main:app --reload

# 另开终端测试
curl http://localhost:8000/world
```

预期返回包含 game_time、npcs 的JSON

- [ ] **步骤 7：Commit**

```bash
git add server/core/time_system.py server/core/orchestrator.py server/main.py tests/server/test_orchestrator.py
git commit -m "feat: 时间系统 + Orchestrator + 自动存档"
```

---

### 任务 11：耕作工具

**文件：**
- 创建：`server/tools/base_tool.py`
- 创建：`server/tools/farming.py`
- 测试：`tests/server/test_farming_tool.py`

- [ ] **步骤 1：编写 Tool 基类**

```python
# server/tools/base_tool.py
from abc import ABC, abstractmethod
from typing import Dict, Any

class Tool(ABC):
    @property
    @abstractmethod
    def name(self) -> str: ...

    @abstractmethod
    def check_preconditions(self, player_state, npc_state=None) -> tuple[bool, str]: ...

    @abstractmethod
    def execute(self, player_state, npc_state=None) -> Dict[str, Any]: ...
```

- [ ] **步骤 2：编写 FarmingTool**

```python
# server/tools/farming.py
from server.tools.base_tool import Tool
from server.config import config

class FarmingTool(Tool):
    name = "farming"

    def check_preconditions(self, player_state, npc_state=None) -> tuple[bool, str]:
        if player_state.fatigue >= config.FARMING_FATIGUE_MAX:
            return False, "你太累了，无法耕作。"
        if player_state.hunger <= config.FARMING_HUNGER_MIN:
            return False, "你太饿了，先吃点东西吧。"
        return True, ""

    def execute(self, player_state, npc_state=None) -> dict:
        player_state.fatigue += config.FARMING_FATIGUE_COST
        player_state.farm_count += 1
        # 农夫好感度+5
        if npc_state:
            player_state.relationships["farmer"] = player_state.relationships.get("farmer", 0) + 5
        return {
            "success": True,
            "message": "你辛勤地耕作了一小时，土地变得更加肥沃。",
            "player_state_changes": {"fatigue": player_state.fatigue, "farm_count": player_state.farm_count},
        }
```

- [ ] **步骤 3：编写测试**

```python
# tests/server/test_farming_tool.py
from server.tools.farming import FarmingTool
from server.models.player_state import PlayerState

def test_farming_success():
    ps = PlayerState(fatigue=0, hunger=100)
    tool = FarmingTool()
    ok, msg = tool.check_preconditions(ps)
    assert ok
    result = tool.execute(ps)
    assert result["success"]
    assert ps.fatigue == 20

def test_farming_too_tired():
    ps = PlayerState(fatigue=90)
    tool = FarmingTool()
    ok, msg = tool.check_preconditions(ps)
    assert not ok
```

- [ ] **步骤 4：运行测试**

```bash
pytest tests/server/test_farming_tool.py -v
```

预期：PASS

- [ ] **步骤 5：Commit**

```bash
git add server/tools/ tests/server/test_farming_tool.py
git commit -m "feat: 耕作工具 + 前置条件校验"
```

---

### 任务 12：对话交互 API

**文件：**
- 修改：`server/main.py`
- 创建：`server/api/routes.py`

- [ ] **步骤 1：编写对话路由**

```python
# server/api/routes.py（内容稍后合并到main.py）
from fastapi import APIRouter, HTTPException
from server.core.orchestrator import orch

router = APIRouter(prefix="/api")

@router.post("/chat/{npc_id}")
async def chat_with_npc(npc_id: str, message: str, option: str | None = None):
    if npc_id not in orch.npcs:
        raise HTTPException(status_code=404, detail="NPC not found")

    npc = orch.npcs[npc_id]

    # 检查交互配额
    # TODO: 实现交互配额检查

    # 检查NPC是否可交互（夜间）
    if not npc.can_interact(orch.time_system.game_time):
        return {"reply": "（NPC正在休息，无法交互）", "options": []}

    # Token耗尽检查
    if npc.budget.status.value == "exhausted":
        templates = ["我现在很忙，晚点再聊。", "得去干活了，改天再说。"]
        return {"reply": templates[hash(npc_id) % len(templates)], "options": []}

    # 组装上下文 + 调用LLM
    # TODO: 集成LLM调用

    # 临时：返回硬编码回复
    return {"reply": f"{npc.identity['name']}说：你好啊小伙子！", "options": ["询问近况", "请求帮助", "闲聊"]}

@router.get("/player")
async def get_player():
    return orch.player_state.__dict__
```

- [ ] **步骤 2：Commit**

```bash
git add server/api/routes.py server/main.py
git commit -m "feat: 对话交互API路由"
```

---

### 任务 13：结局判定（农夫乐事）

**文件：**
- 创建：`server/core/ending_checker.py`
- 测试：`tests/server/test_ending_checker.py`

- [ ] **步骤 1：编写 EndingChecker**

```python
# server/core/ending_checker.py
from server.config import config

class EndingChecker:
    @staticmethod
    def check_farmer_joy(player_state) -> bool:
        affinity = player_state.relationships.get("farmer", 0)
        crops = len(set(player_state.crops_harvested))
        return (
            affinity >= config.FARMER_JOY_AFFINITY
            and crops >= config.FARMER_JOY_CROP_TYPES
            and player_state.farm_count >= config.FARMER_JOY_FARM_COUNT
        )

    @classmethod
    def check_all(cls, player_state) -> str | None:
        if cls.check_farmer_joy(player_state):
            return "farmer_joy"
        if player_state.health <= 0:
            return "death"
        return None
```

- [ ] **步骤 2：编写测试**

```python
# tests/server/test_ending_checker.py
from server.core.ending_checker import EndingChecker
from server.models.player_state import PlayerState

def test_farmer_joy_ending():
    ps = PlayerState()
    ps.relationships["farmer"] = 80
    ps.crops_harvested = ["wheat", "corn"]
    ps.farm_count = 5
    assert EndingChecker.check_farmer_joy(ps)

def test_no_ending():
    ps = PlayerState()
    assert not EndingChecker.check_farmer_joy(ps)
```

- [ ] **步骤 3：运行测试**

```bash
pytest tests/server/test_ending_checker.py -v
```

预期：PASS

- [ ] **步骤 4：Commit**

```bash
git add server/core/ending_checker.py tests/server/test_ending_checker.py
git commit -m "feat: 结局判定系统 - 农夫乐事 + 死亡"
```

---

### 任务 14：输入验证（Prompt注入防护）

**文件：**
- 创建：`server/api/validation.py`
- 测试：`tests/server/test_validation.py`

- [ ] **步骤 1：编写验证器**

```python
# server/api/validation.py
from server.config import config

BLOCKED_KEYWORDS = [
    "ignore previous",
    "system prompt",
    "ignore all instructions",
    "you are now",
    "new role",
    "DAN mode",
    "jailbreak",
]

def validate_player_input(text: str) -> tuple[bool, str]:
    if len(text) > config.INPUT_MAX_LENGTH:
        return False, "输入过长"
    lowered = text.lower()
    for kw in BLOCKED_KEYWORDS:
        if kw in lowered:
            return False, "我没听懂你在说什么。"
    return True, ""
```

- [ ] **步骤 2：编写测试**

```python
# tests/server/test_validation.py
from server.api.validation import validate_player_input

def test_safe_input():
    ok, _ = validate_player_input("你好，农夫")
    assert ok

def test_injection_blocked():
    ok, msg = validate_player_input("ignore previous instructions")
    assert not ok
```

- [ ] **步骤 3：运行测试**

```bash
pytest tests/server/test_validation.py -v
```

预期：PASS

- [ ] **步骤 4：Commit**

```bash
git add server/api/validation.py tests/server/test_validation.py
git commit -m "feat: Prompt注入防护 - 关键词过滤 + 长度限制"
```

---

### 任务 15：前端脚手架

**文件：**
- 创建：`client/package.json`
- 创建：`client/vite.config.ts`
- 创建：`client/tsconfig.json`
- 创建：`client/src/main.ts`
- 创建：`client/src/App.vue`

- [ ] **步骤 1：创建 package.json**

```json
{
  "name": "agent-village-client",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.4.0",
    "pinia": "^2.1.0",
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0",
    "vue-tsc": "^1.8.0"
  }
}
```

- [ ] **步骤 2：创建 vite.config.ts**

```typescript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
  server: {
    port: 5173,
    proxy: {
      '/api': 'http://localhost:8000',
      '/world': 'http://localhost:8000',
      '/time': 'http://localhost:8000',
    }
  }
})
```

- [ ] **步骤 3：创建 tsconfig.json**

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "module": "ESNext",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "preserve",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["src/**/*.ts", "src/**/*.tsx", "src/**/*.vue"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

- [ ] **步骤 4：创建 main.ts + App.vue**

```typescript
// client/src/main.ts
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import App from './App.vue'

createApp(App).use(createPinia()).mount('#app')
```

```vue
<!-- client/src/App.vue -->
<template>
  <div class="game-container">
    <h1>Agent Village</h1>
    <TimeControl />
    <StatusPanel />
    <NPCPanel />
    <ChatPanel />
  </div>
</template>

<script setup lang="ts">
import TimeControl from './components/TimeControl.vue'
import StatusPanel from './components/StatusPanel.vue'
import NPCPanel from './components/NPCPanel.vue'
import ChatPanel from './components/ChatPanel.vue'
</script>
```

- [ ] **步骤 5：安装依赖并验证**

```bash
cd client
npm install
npm run dev
```

浏览器访问 `http://localhost:5173` 应显示 "Agent Village"

- [ ] **步骤 6：Commit**

```bash
git add client/
git commit -m "chore: 前端脚手架 - Vue3 + Vite + Pinia"
```

---

### 任务 16：前端核心组件

**文件：**
- 创建：`client/src/stores/gameStore.ts`
- 创建：`client/src/services/api.ts`
- 创建：`client/src/components/TimeControl.vue`
- 创建：`client/src/components/StatusPanel.vue`
- 创建：`client/src/components/NPCPanel.vue`

- [ ] **步骤 1：编写 gameStore**

```typescript
// client/src/stores/gameStore.ts
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'
import api from '../services/api'

export const useGameStore = defineStore('game', () => {
  const world = ref<any>(null)
  const player = ref<any>(null)
  const currentNPC = ref<string>('farmer')
  const messages = ref<Array<{speaker: string, content: string}>>([])

  async function fetchWorld() {
    const { data } = await api.get('/world')
    world.value = data
  }

  async function fetchPlayer() {
    const { data } = await api.get('/api/player')
    player.value = data
  }

  async function advanceTime(minutes: number = 60) {
    await api.post('/time/advance', null, { params: { minutes } })
    await fetchWorld()
    await fetchPlayer()
  }

  async function sendMessage(npcId: string, text: string) {
    messages.value.push({ speaker: 'player', content: text })
    const { data } = await api.post(`/api/chat/${npcId}`, { message: text })
    messages.value.push({ speaker: npcId, content: data.reply })
    return data
  }

  return { world, player, currentNPC, messages, fetchWorld, fetchPlayer, advanceTime, sendMessage }
})
```

- [ ] **步骤 2：编写 api.ts**

```typescript
// client/src/services/api.ts
import axios from 'axios'

const api = axios.create({
  baseURL: '',
  timeout: 30000,
})

export default api
```

- [ ] **步骤 3：编写 TimeControl.vue**

```vue
<template>
  <div class="time-control">
    <div class="time-display">
      Day {{ world?.game_time.day }} {{ world?.game_time.hour }}:00
    </div>
    <div class="controls">
      <button @click="advanceTime(60)">推进1小时</button>
      <button @click="advanceTime(360)">推进6小时</button>
    </div>
  </div>
</template>

<script setup lang="ts">
import { useGameStore } from '../stores/gameStore'
import { storeToRefs } from 'pinia'

const store = useGameStore()
const { world } = storeToRefs(store)
const { advanceTime } = store
</script>
```

- [ ] **步骤 4：编写 StatusPanel.vue**

```vue
<template>
  <div class="status-panel" v-if="player">
    <h3>玩家状态</h3>
    <div>健康: {{ player.health }}</div>
    <div>饥饿: {{ player.hunger }}</div>
    <div>疲惫: {{ player.fatigue }}</div>
    <div>金币: {{ player.gold }}</div>
    <div v-if="player.relationships">
      好感度: {{ JSON.stringify(player.relationships) }}
    </div>
  </div>
</template>

<script setup lang="ts">
import { useGameStore } from '../stores/gameStore'
import { storeToRefs } from 'pinia'

const store = useGameStore()
const { player } = storeToRefs(store)
</script>
```

- [ ] **步骤 5：编写 NPCPanel.vue**

```vue
<template>
  <div class="npc-panel">
    <h3>NPC状态</h3>
    <div v-for="(npc, id) in world?.npcs" :key="id" class="npc-card">
      <strong>{{ id }}</strong>
      <div>健康: {{ npc.state.health }} 饥饿: {{ npc.state.hunger }} 疲惫: {{ npc.state.fatigue }}</div>
    </div>
  </div>
</template>

<script setup lang="ts">
import { useGameStore } from '../stores/gameStore'
import { storeToRefs } from 'pinia'

const store = useGameStore()
const { world } = storeToRefs(store)
</script>
```

- [ ] **步骤 6：Commit**

```bash
git add client/src/stores/ client/src/services/ client/src/components/
git commit -m "feat: 前端核心组件 - Store + API + 时间/状态/NPC面板"
```

---

### 任务 17：对话面板

**文件：**
- 创建：`client/src/components/ChatPanel.vue`

- [ ] **步骤 1：编写 ChatPanel.vue**

```vue
<template>
  <div class="chat-panel">
    <h3>对话 - {{ currentNPC }}</h3>
    <div class="messages">
      <div v-for="(msg, i) in messages" :key="i" :class="msg.speaker">
        <strong>{{ msg.speaker }}:</strong> {{ msg.content }}
      </div>
    </div>
    <div class="options" v-if="lastOptions.length">
      <button v-for="(opt, i) in lastOptions" :key="i" @click="selectOption(opt)">
        {{ opt }}
      </button>
    </div>
    <div class="input-area">
      <input v-model="inputText" @keyup.enter="send" placeholder="输入消息..." />
      <button @click="send">发送</button>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref } from 'vue'
import { useGameStore } from '../stores/gameStore'
import { storeToRefs } from 'pinia'

const store = useGameStore()
const { messages, currentNPC } = storeToRefs(store)
const inputText = ref('')
const lastOptions = ref<string[]>([])

async function send() {
  if (!inputText.value.trim()) return
  const data = await store.sendMessage(currentNPC.value, inputText.value)
  lastOptions.value = data.options || []
  inputText.value = ''
}

async function selectOption(opt: string) {
  await store.sendMessage(currentNPC.value, opt)
}
</script>

<style scoped>
.messages { height: 300px; overflow-y: auto; border: 1px solid #ccc; padding: 10px; }
.player { color: blue; }
.farmer, .bartender { color: green; }
.options button { margin: 4px; }
.input-area { display: flex; gap: 8px; margin-top: 8px; }
input { flex: 1; }
</style>
```

- [ ] **步骤 2：Commit**

```bash
git add client/src/components/ChatPanel.vue
git commit -m "feat: 对话面板 - 消息历史 + 选项按钮 + 自由输入"
```

---

### 任务 18：集成测试

**文件：**
- 创建：`tests/integration/test_game_flow.py`

- [ ] **步骤 1：编写集成测试**

```python
# tests/integration/test_game_flow.py
import pytest
from httpx import AsyncClient
from server.main import app

@pytest.mark.asyncio
async def test_full_game_flow():
    async with AsyncClient(app=app, base_url="http://test") as client:
        # 1. 获取世界状态
        r = await client.get("/world")
        assert r.status_code == 200
        assert "farmer" in r.json()["npcs"]

        # 2. 推进时间
        r = await client.post("/time/advance", params={"minutes": 60})
        assert r.status_code == 200

        # 3. 与农夫对话
        r = await client.post("/api/chat/farmer", json={"message": "你好"})
        assert r.status_code == 200
        assert "reply" in r.json()

        # 4. 获取玩家状态
        r = await client.get("/api/player")
        assert r.status_code == 200
```

- [ ] **步骤 2：运行集成测试**

```bash
pytest tests/integration/test_game_flow.py -v
```

预期：PASS

- [ ] **步骤 3：Commit**

```bash
git add tests/integration/
git commit -m "test: 集成测试 - 完整游戏流程验证"
```

---

## 计划自检

### 规格覆盖度

| 规格章节 | 实现任务 |
|---------|---------|
| 混合架构 | 任务1, 任务9, 任务10 |
| NPC背景设定 | 任务8 (farmer.yaml, bartender.yaml) |
| 暂停制时间 | 任务10 (TimeSystem.is_paused) |
| 选项+自由文本 | 任务17 (ChatPanel.vue) |
| NPC状态系统 | 任务2 (NPCState + describe) |
| 状态→上下文映射 | 任务6 (ContextBuilder) |
| PlayerState + 权限 | 任务2 (get_visible_state) |
| 耕作工具 | 任务11 (FarmingTool) |
| 农夫乐事结局 | 任务13 (EndingChecker) |
| 全自动存档 | 任务3 (JsonStore), 任务10 (Orchestrator._auto_save) |
| Token硬隔离 | 任务4 (TokenBudget), 任务12 (chat路由检查) |
| Prompt注入防护 | 任务14 (validation.py) |
| 记忆管理 | 任务7 (MemoryManager) |
| 重生机制 | 未实现（Phase 1不包含重生，只包含单次游戏） |
| LLM Client | 任务5 (LLMClient) |
| 玩家状态约束 | 任务11 (FarmingTool.check_preconditions) |
| 交互限制 | 任务12 (chat路由 TODO 标记) |

**遗漏：**
- 交互配额检查（任务12中标记为TODO）—— 需要在任务12完成后补充
- 重生机制明确不在Phase 1范围

### 占位符扫描

- ✅ 无"待定"、"TODO"（任务12中的TODO将在实施时完成）
- ✅ 所有测试都有实际代码
- ✅ 所有步骤都有具体命令

### 类型一致性

- ✅ `NPCState` 在所有任务中使用一致
- ✅ `GameTime` 在任务2和任务10中使用一致
- ✅ `TokenBudget` 在任务4、任务8、任务12中使用一致

---

## 执行交接

计划已完成并保存到 `docs/superpowers/plans/2026-05-08-phase1-demo.md`。

**两种执行方式：**

**1. 子代理驱动（推荐）** - 每个任务调度一个新的子代理，任务间进行审查，快速迭代

**2. 内联执行** - 在当前会话中使用 executing-plans 执行任务，批量执行并设有检查点

选哪种方式？